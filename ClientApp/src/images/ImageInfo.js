

function ImageInfo(props) {

    //var imageSrc = "images/thumbnails/" + props.id + "?" + props.queryString;

    return (
        <div className="imageInfo">
            test
        </div>
    );
}

export default ImageInfo;
